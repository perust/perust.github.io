{
  "name": "slowave-blog-comments-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 execute slowave_blog_comments --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260628.0",
    "wrangler": "^4.23.0"
  }
}
