---
import BaseLayout from '../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
  const modules = import.meta.glob('../../content/blog/*.md', { eager: true });
  return Object.entries(modules).map(([path, post]: [string, any]) => ({
    params: { slug: path.split('/').pop()?.replace('.md', '') },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = post;
const { title, description, date, category, tags = [] } = post.frontmatter;
---
<BaseLayout title={`${title} · 임승우 블로그`} description={description} canonical={Astro.url.pathname} ogType="article">
  <main class="post-layout">
    <article class="post-article">
      <p class="section-kicker">{category}</p>
      <h1>{title}</h1>
      <p class="post-meta"><time datetime={new Date(date).toISOString()}>{new Date(date).toLocaleDateString('ko-KR')}</time></p>
      <Content />
    </article>
    <aside class="sidebar-card" aria-label="블로그 안내">
      <h2 style="font-size:1.2rem">Seungwoo Blog</h2>
      <p>아이디어를 제품으로 만들며 배운 것을 기록합니다.</p>
      <ul class="post-tags">{tags.map((tag: string) => <li>{tag}</li>)}</ul>
    </aside>
  </main>
</BaseLayout>
